<!-- by alex january 2015 -->
<!DOCTYPE html>
<html lang="en">
<head>
    <title>mood</title>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <script type="text/javascript" src="//cdnjs.cloudflare.com/ajax/libs/vis/3.11.0/vis.min.js"></script>
    <script type="text/javascript" src="//cdnjs.cloudflare.com/ajax/libs/qtip2/2.2.1/jquery.qtip.js"></script>
    <link rel="stylesheet" type="text/css" href="//cdnjs.cloudflare.com/ajax/libs/vis/3.11.0/vis.min.css">
    <link rel="stylesheet" type="text/css" href="//cdnjs.cloudflare.com/ajax/libs/qtip2/2.2.1/jquery.qtip.css">
    <%= csrf_meta_tags %>
</head>
<body class="<%= yield(:class) %>">
    <header>
        <h1><%= link_to image_tag("http://s.goose.im/emoji/emoji_u1f30a.png"), root_url %></h1>
        <nav>
            <% if logged_in? %>
                <%= link_to "Log out", logout_path, method: "delete" %>
                <%= link_to "History", history_path %>
                <a>Hi, <%= current_user.username %></a>
            <% else %>
                <%= link_to "Log in", login_path %>
                <%= link_to "Sign up", signup_path %>
            <% end %>
        </nav>
    </header>

    <% flash.each do |message_type, message| %>
        <div class="flash flash-<%= message_type %>"><%= message %></div>
    <% end %>

    <%= yield %>

</body>
</html>
